{
  "name": "nomad-town-square",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "heroku-postbuild": "cd ./client && npx parcel build index.html",
    "start": "node server/index"
  },
  "devDependencies": {
    "parcel": "^2.0.1"
  },
  "dependencies": {
    "ethers": "^5.5.1",
    "express": "^4.17.1"
  }
}
